# Simple Voice Assistant

import pyttsx3
import speech_recognition as sr
import webbrowser
import datetime

# Initialize voice engine
engine = pyttsx3.init()

# Function to speak
def speak(text):
    engine.say(text)
    engine.runAndWait()
